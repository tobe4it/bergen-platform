# Bergen Platform daytrading collector patch

Files in this bundle are laid out relative to the `bergen-platform` repository root.

Suggested commit:

```text
feat: add daytrading market-data collector
```

The patch intentionally does not modify `ansible/inventory.yml` because the real target host/IP is not known here.
